"""Domain content modules for CAST (missions, simulations catalogue).

Kept separate from services/ (behaviour) and backend/ (transport) so the
Learning Path and Practice Lab content can be extended independently.

Future network/ and ids/ modules (VISTA / Sentinel integration) will live
as sibling packages at the project root, not inside this package — do not
add cross-imports here until that integration actually exists.
"""
