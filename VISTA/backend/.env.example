# Copy this file to .env and adjust as needed
FLASK_ENV=development
FLASK_DEBUG=1
HOST=127.0.0.1
PORT=5000
CORS_ORIGIN=http://localhost:5173
# Max packets kept in memory per session before oldest are dropped
MAX_PACKET_BUFFER=5000
