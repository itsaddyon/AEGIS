# Extension point: Intrusion Detection System engine internals
#
# Separate from app/alerts (the API surface), this package will hold
# the actual detection algorithms (signature matching, anomaly
# scoring, etc.) in a future version. Kept empty and decoupled today
# so it can be developed and tested independently of the API layer.
